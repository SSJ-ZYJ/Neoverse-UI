---
'@neoverse-ui/tokens': minor
'@neoverse-ui/tailwind': minor
---
Unify dark-mode navigation active state with regular controls: a new `--neoverse-navigation-item-active-fill` token bakes the dense glass plate under the accent sheen (transparent in light), while the consumer dock keeps its playground-only composition and consumes the shared navigation active-state tokens. The mint foreground and selected indicator bar keep the navigation identity.
