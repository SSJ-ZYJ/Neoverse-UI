---
'@neoverse-ui/glass-runtime': minor
---

Add an optional shared static WebGL edge-refraction renderer for Glass materials. CSS backdrop sampling and the existing CSS edge field remain the fallback when WebGL is unavailable, the context is lost, or reduced transparency is requested.
