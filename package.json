{
  "name": "neoverse-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun run --filter @neoverse-ui/tokens build && bun run --filter @neoverse-ui/motion build && bun run --filter @neoverse-ui/glass-runtime build && bun run --filter @neoverse-ui/vue build && bun run --filter @neoverse-ui/tailwind build && bun run --filter @neoverse-ui/playground build",
    "build:types": "bun run --filter @neoverse-ui/tokens build && bun run --filter @neoverse-ui/motion build && bun run --filter @neoverse-ui/glass-runtime build && bun run --filter @neoverse-ui/vue build",
    "test:visual": "bun run build && playwright test",
    "test:visual:update": "bun run build && playwright test --update-snapshots",
    "lint": "bun run lint:biome && bun run lint:style && bun run lint:tailwind",
    "lint:biome": "biome lint .",
    "lint:style": "stylelint \"packages/**/*.css\" \"apps/**/*.css\"",
    "lint:tailwind": "bun scripts/check-tailwind-usage.ts",
    "typecheck": "bun run build:types && bun run --filter @neoverse-ui/tokens typecheck && bun run --filter @neoverse-ui/motion typecheck && bun run --filter @neoverse-ui/glass-runtime typecheck && bun run --filter @neoverse-ui/vue typecheck && bun run --filter @neoverse-ui/playground typecheck && tsc --project tsconfig.json --noEmit",
    "test": "bun run --filter '@neoverse-ui/*' --if-present --sequential test",
    "format": "biome format --write .",
    "check": "biome check --write . && bun run lint:style && bun run lint:tailwind && bun run typecheck && bun run test",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun run build && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.10",
    "@changesets/cli": "^2.27.0",
    "@playwright/test": "1.60.0",
    "@types/node": "^22.0.0",
    "bun-types": "1.4.0",
    "stylelint": "^16.0.0",
    "typescript": "^5.6.0"
  }
}
