---
'@neoverse-ui/tokens': minor
'@neoverse-ui/tailwind': minor
---

Unify dark-mode navigation active state with regular controls: a new `--neoverse-navigation-item-active-fill` token bakes the dense glass plate under the accent sheen (transparent in light), and the consumer dock reuses the shared navigation tokens instead of hardcoded shadows. The mint foreground and selected indicator bar keep the navigation identity.
